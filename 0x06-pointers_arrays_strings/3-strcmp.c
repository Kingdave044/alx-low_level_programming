#include "main.h"
#include <string.h>
/**
 * _strcmp - compares two strings
 * @s!: the first string
 * @s2: the second string
 * Return: an integer
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
